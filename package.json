{
  "name": "h2s-dosai",
  "private": true,
  "version": "0.1.0",
  "description": "SIH 26118 — Passive H2S exposure dosimeter kiosk + admin dashboard (MRPL)",
  "scripts": {
    "dev": "concurrently -n api,web,ml -c cyan,magenta,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix ml\"",
    "dev:web": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && python -m pip install -r ml/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
